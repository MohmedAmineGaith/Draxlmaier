services:
  mongodb:
    image: mongo:7
    container_name: draxlmaier-mongo
    ports:
      - "27017:27017"
    volumes:
      - draxlmaier_mongo_data:/data/db
    restart: unless-stopped

volumes:
  draxlmaier_mongo_data:
